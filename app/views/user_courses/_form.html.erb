<%= form_with(model: user_course, local: true) do |form| %>
  <% if user_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_course.errors.count, "error") %> prohibited this user_course from being saved:</h2>

      <ul>
      <% user_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :user_course_user_id %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.number_field :course_id, id: :user_course_course_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status, id: :user_course_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
